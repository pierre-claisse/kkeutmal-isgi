{
  "name": "kkeutmal-isgi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Jeu PWA de 끝말잇기 (word chain coréen)",
  "scripts": {
    "build:dict": "node scripts/build-dict.mjs",
    "prebuild": "node scripts/build-dict.mjs",
    "dev": "vite",
    "build": "tsc -b --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.5"
  }
}
